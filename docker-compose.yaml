version: "3"
services:
  lpdnet-tao-toolkit:
    container_name: lpdnet-tao-toolkit
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./:/app/src
    working_dir: /app/src
    tty: true
